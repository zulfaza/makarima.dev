{
  "name": "makarima-dev-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter web dev",
    "build": "bun run --filter web build",
    "deploy": "bun run --filter web deploy",
    "preview": "bun run --filter web preview",
    "test": "bun run --filter web test",
    "lint": "bun run --filter web lint",
    "lint:fix": "bun run --filter web lint:fix",
    "format": "bun run --filter web format",
    "format:check": "bun run --filter web format:check",
    "typecheck": "bun run --filter web typecheck",
    "check": "bun run --filter web check"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
